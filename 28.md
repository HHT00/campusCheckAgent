# 2026-07-29 工作总结

## 项目背景

校园考勤助手（edu_check），Spring Boot + MyBatis-Plus 后端，微信小程序前端。覆盖上课打卡、查寝打卡、实习打卡、智能问答等功能。项目处于早期开发阶段，后端大量接口未实现，数据分析能力为零。

---

## 一、项目全面分析

### 输出文档

| 文件 | 内容 | 状态 |
|------|------|------|
| `complete.md` | 后端缺失接口清单（约 34 个接口，7 大模块） | 已输出 |
| `design.md` | 数据驱动的 AI Agent 改进方案（数据分析流程 + Agent 架构） | 已输出 |

### 核心发现

**complete.md** — 34 个接口未开发：
- 公告管理 7 个、请假管理 8 个、实习打卡 6 个（完全缺失，P0）
- 上课签到缺 2 个（checkin 提交 + 课表查询）
- 教师端缺 6 个（动态码、学生管理、签到总结）
- 意见反馈 3 个、智能问答缺 1 个、仪表盘硬编码 1 处

**design.md 的三次迭代：**

| 版本 | 问题 | 改进 |
|------|------|------|
| v1 | 30 分钟批处理管道，过度设计 | 改为事件驱动实时更新 |
| v2 | ReAct 手写 `[TOOL]` 文本解析，脆弱 | 改为 LangChain4j `@Tool` + function calling |
| v3 | 只分析了 class_checkin 和 dorm_checkin 两张表 | 覆盖全部 15 张表的字段分析 |

---

## 二、数据分析流程设计

### 架构

```
采集层（业务代码触发）
  ┣━ 查寝打卡 → updateOnDormCheckin()
  ┣━ 上课签到 → updateOnClassCheckin()
  ┣━ 实习打卡 → updateOnInternCheckin()
  ┗━ 请假审批 → updateOnLeaveApproved()
       ↓ 实时 (同一事务, <10ms)
处理层（FeatureDailyService）
  ┣━ UPSERT feature_daily 行
  ┣━ 重算 7 天滚动窗口
  ┗━ 复制前日交叉特征
       ↓ 每天凌晨
校准层（FeatureDailyScheduler）
  ┣━ 00:30 初始化今日空记录
  ┣━ 01:00 计算 class_required
  ┣━ 01:30 计算 is_morning_class
  ┣━ 02:00 补全 leave_today
  ┣━ 02:30 刷新 5 个滚动窗口字段
  ┗━ 03:00 复制 prev_dorm_late
       ↓
存储层（MySQL feature_daily 表）
  每学生每天一条，约 20 个特征字段
```

### 特征宽表设计

`feature_daily` 表包含 5 类特征：

| 类别 | 字段 | 更新方式 |
|------|------|---------|
| 基础计数 | class_present, class_late, class_absent, dorm_done, dorm_time | 实时写 |
| 业务标记 | dorm_building, intern_done, leave_today | 实时写 |
| 交叉特征 | prev_dorm_late, is_morning_class, is_monday, is_friday | 定时/实时 |
| 滚动窗口 | absent_last_7d, late_last_7d, dorm_late_last_7d, leave_count_30d, monday_sick_30d | 实时重算+定时刷新 |
| 行为特征 | streak_days | 定时计算 |

---

## 三、Agent 架构设计

### 选型

| 组件 | 方案 | 说明 |
|------|------|------|
| Agent 引擎 | LangChain4j AiServices | 内置 ReAct 循环、tool call 解析 |
| 工具定义 | `@Tool` 注解 | 自动生成 FunctionDeclaration |
| 短期记忆 | 前端传 history + LangChain4j ChatMemory | 会话级 |
| 长期记忆 | 自定义 `user_memory` 表 + MemoryService | 跨会话，存请假模式/偏好 |
| 权限校验 | `@Tool` 方法内部硬拦截 | 不依赖 LLM 自觉 |
| RAG | pgvector 混合检索（关键词 + 向量） | 作为 `search_knowledge` 工具 |

### Agent 工具列表（7 个）

| 工具 | 功能 | 读写 |
|------|------|------|
| search_knowledge | 检索 FAQ 知识库 | 只读 |
| query_schedule | 查课表 | 只读 |
| query_attendance | 查 feature_daily | 只读，学生限查自己 |
| query_risk | 查 alert_event | 只读，教师可查全班 |
| submit_leave | 提交请假 | 写，需确认 |
| notify_teacher | 通知教师 | 写，限教师端 |
| ask_user | 追问用户 | 纯对话 |

### 请假场景 ReAct 流程

```
用户: "帮我请个假"
→ Agent 追问类型和时间
用户: "明天发烧了，请病假一天"
→ Agent 调 query_schedule → 发现 2 节课
→ Agent 调 query_attendance → 发现 3 次周一病假记录
→ Agent 调 submit_leave → 转人工审批
→ Agent 调 notify_teacher → 通知教师
→ Agent 回复："已提交，检测到多次周一病假已转人工审批"
```

---

## 四、代码实现

### 新建文件（8 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `entity/FeatureDaily.java` | 81 行 | 特征宽表实体，@TableField 标注带数字字段 |
| `mapper/FeatureDailyMapper.java` | 56 行 | 5 个自定义 SQL 方法（含 @Select/@Update） |
| `service/feature/FeatureDailyService.java` | 176 行 | 核心服务：4 个实时更新方法 + 初始化 + 滚动窗口计算 |
| `scheduler/FeatureDailyScheduler.java` | 144 行 | 6 个定时任务（00:30～03:00） |
| `complete.md` | — | 缺失接口清单 |
| `design.md` | — | 改进方案文档 |

### 修改文件（2 个）

| 文件 | 改动 |
|------|------|
| `controller/DormCheckinController.java` | 注入 FeatureDailyService，查寝打卡后追加特征更新调用；修复 2 个已有 bug（缺 return、faceImage 空指针） |
| `entity/FeatureDaily.java` | 追加 weekDay、internDone、leaveToday、dormBuilding、streakDays、leaveCount30d、mondaySick30d、lateLast7d、dormLateLast7d、updatedAt |

### 删除文件（1 个）

| 文件 | 原因 |
|------|------|
| `service/feature/FeatureUpdateManager.java` | JdbcTemplate 方案替换为 MyBatis-Plus 方案 |

### 数据库变更

```sql
-- 新建表
CREATE TABLE feature_daily ( ...约 20 个字段... );

-- 追加字段（6 个）
ALTER TABLE feature_daily ADD dorm_building;
ALTER TABLE feature_daily ADD streak_days;
ALTER TABLE feature_daily ADD leave_count_30d;
ALTER TABLE feature_daily ADD monday_sick_30d;
ALTER TABLE feature_daily ADD late_last_7d;
ALTER TABLE feature_daily ADD dorm_late_last_7d;
```

---

## 五、SQL 字段映射踩坑

`absentLast7d` 这样的驼峰字段，MyBatis-Plus 自动转下划线为 `absent_last7d`，但数据库列名是 `absent_last_7d`。需要加 `@TableField("absent_last_7d")` 显式指定列名。

涉及 5 个带数字结尾的字段：absentLast7d、leaveCount30d、mondaySick30d、lateLast7d、dormLateLast7d。

---

## 六、下一步建议

| 优先级 | 任务 | 前置条件 |
|--------|------|---------|
| P0 | 把 updateOnClassCheckin 接入 CourseCheckinController | 现有 /api/course/today 可接旷课判定 |
| P0 | 建 alert_rule + alert_event 规则引擎 | feature_daily 已有数据 |
| P1 | 完成请假模块（leave_application 表 + 接口） | 规则引擎需要 leave 数据 |
| P1 | 教师看板数据接口（从 feature_daily 聚合） | feature_daily 数据积累后 |
| P2 | LangChain4j Agent 实现 | 工具定义 + 长期记忆 |
| P2 | pgvector RAG 增强 | 知识库管理闭环 |
